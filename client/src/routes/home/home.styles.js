import styled from "@emotion/styled";
import { Button, Heading } from "@chakra-ui/react";

export const NoContactHeading = styled(Heading)({
  position: "absolute",
  top: "200%",
  left: "25%",
  fontSize: "70px",
  color: "lightgrey",
});

export const AddContactButton = styled(Button)({
  fontSize: "30px",
  padding: "30px",
});
